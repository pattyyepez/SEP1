package Main;

import View.Start;
import javafx.application.Application;

/**
 * The class that launches the GUI of the program.
 *
 * @author Group SiedemSyvSiete
 * @version 1.0
 */
public class BobsCoGUITest {

  /**
   * The method that launches the application.
   *
   * @param args    We'll be honest, we're quite bemused by this one.
   */
  public static void main(String[] args){
    Application.launch(Start.class);
  }
}
